package cn.jia.chat.service;

/** Server-side source selected for a resolved human sender display name. */
public enum DisplayNameSource {
    NICKNAME,
    USERNAME,
    CONTEXT_USERNAME,
    JIACN,
    FALLBACK
}
